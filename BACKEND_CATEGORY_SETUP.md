# Backend Category Setup & Verification Guide

এই গাইডটিতে আপনার ব্যাকএন্ড সার্ভারে ডায়নামিক ক্যাটাগরির জন্য কী কী পরিবর্তন করতে হবে এবং কীভাবে সম্পূর্ণ সিস্টেমটি পরীক্ষা (Test) করবেন তা বিস্তারিত বলা হলো।

---

## ১. Backend এ যা যা করতে হবে

### ধাপ ১: Mongoose Item Model চেক করুন
আপনার ব্যাকএন্ড ফোল্ডারের (যেমন: `shey-pos-server/models/itemsModel.js` বা `itemModel.js`) ফাইলে যান।

**বর্তমান কোড চেক করুন:**
যদি `category` ফিল্ডে কোনো `enum` দেওয়া থাকে, তবে সেটি মুছে ফেলুন:

```javascript
// ❌ পূর্বে যদি এমন সীমাবদ্ধতা (enum) থাকে:
category: {
  type: String,
  enum: ['fruits', 'vegetables', 'meat'], // এটি থাকলে নতুন ক্যাটাগরি সেভ হতে দেবে না
  required: true
}

//  পরিবর্তন করে সাধারণ String বানিয়ে দিন:
category: {
  type: String,
  required: true,
  trim: true
}
```
> **কেন?** সাধারণ `String` থাকলে ইউজার ফ্রন্টএন্ড থেকে যেকোনো নতুন ক্যাটাগরি (যেমন: `Dairy`, `Bakery`, `Beverages`, `Snacks`) পাঠালে মঙ্গোডিবি কোনো ত্রুটি ছাড়াই তা সেভ করে নেবে।

---

### ধাপ ২: (ঐচ্ছিক কিন্তু দারুণ) Distinct Categories API যোগ করা
আপনার `itemsRoute.js` (বা আইটেমের রাউট ফাইলে) একটি অতিরিক্ত এন্ডপয়েন্ট রাখতে পারেন যা ডাটাবেজের সব ইউনিক ক্যাটাগরি রিটার্ন করবে:

```javascript
// GET /api/items/get-categories
router.get('/get-categories', async (req, res) => {
  try {
    // MongoDB distinct কুয়েরি স্বয়ংক্রিয়ভাবে সব ইউনিক ক্যাটাগরির তালিকা দেয়
    const categories = await Item.distinct('category');
    res.status(200).send(categories);
  } catch (error) {
    res.status(500).json(error);
  }
});
```

---

## ২. ফ্রন্টএন্ডে ইতিমধ্যে কী কী পরিবর্তন করা হয়েছে?

1. **[src/pages/Items.jsx](file:///c:/Sharif/Shey-POS-client-application/src/pages/Items.jsx):**
   - প্রোডাক্ট অ্যাড/এডিট ফর্মে ড্রপডাউনে **"Add new category"** ইনপুট এবং **"Add"** বাটন যুক্ত করা হয়েছে।
   - ইউজার এক্সিস্টিং ক্যাটাগরিগুলো থেকে সিলেক্ট করতে পারবে, অথবা সরাসরি যেকোনো নতুন ক্যাটাগরির নাম লিখে `Add` ক্লিক করতে পারবে (বা কিবোর্ডে `Enter` চাপতে পারবে)।
   - টেবিলে যেকোনো নতুন ক্যাটাগরির জন্য অটোমেটিক কালারফুল ট্যাগ (`Tag`) রেন্ডার হবে।

2. **[src/pages/Homepage.jsx](file:///c:/Sharif/Shey-POS-client-application/src/pages/Homepage.jsx):**
   - হার্ডকোডেড ৩টি ক্যাটাগরির বদলে এখন ডাটাবেজের সব ইউনিক ক্যাটাগরি স্বয়ংক্রিয়ভাবে ফিল্টার বারে কার্ড আকারে রেন্ডার হবে।
   - জনপ্রিয় ক্যাটাগরিগুলোর (`Dairy`, `Beverages`, `Bakery`, `Snacks`, ইত্যাদি) জন্য আকর্ষণীয় ইমেজ ও আইকন দেওয়া হয়েছে।
   - নতুন কোনো কাস্টম ক্যাটাগরি যোগ করা হলে স্বয়ংক্রিয়ভাবে সেই ক্যাটাগরির প্রোডাক্ট ইমেজটি ক্যাটাগরি কার্ডে দেখাবে।

---

## ৩. কীভাবে টেস্ট বা চেক করবেন (Verification Steps)

১. **ব্যাকএন্ড সার্ভার চালু রাখুন:**
   - নিশ্চিত করুন `node server.js` চালু রয়েছে।

২. **ফ্রন্টএন্ডে Items পেজে যান:**
   - ব্রাউজারে `http://localhost:5173/items` এ যান।
   - **"Add New Product"** বাটনে ক্লিক করুন।
   - ফর্মে নাম, দাম, স্টক দিন।
   - **Category** ড্রপডাউনটি ওপেন করুন।
   - নিচে দেওয়া ইনপুট বক্সে যেকোনো নতুন ক্যাটাগরি টাইপ করুন (যেমন: `Beverages` বা `Dairy` বা `Bakery`)।
   - **"Add"** বাটনে ক্লিক করুন (বা `Enter` চাপুন)। সাথে সাথে ক্যাটাগরি সিলেক্ট হয়ে যাবে।
   - **"Save Product"** বাটনে ক্লিক করুন।

৩. **হোমপেজে চেক করুন (POS Homepage):**
   - এবার ব্রাউজারে `http://localhost:5173/` (হোমপেজ)-এ যান।
   - ক্যাটাগরি বারে লক্ষ্য করুন: আপনার নতুন ক্যাটাগরি কার্ডটি স্বয়ংক্রিয়ভাবে সেখানে চলে এসেছে!
   - নতুন ক্যাটাগরি কার্ডটিতে ক্লিক করুন—দেখবেন সাথে সাথে শুধু ওই ক্যাটাগরির প্রোডাক্টগুলোই ফিল্টার হয়ে দেখাচ্ছে।
